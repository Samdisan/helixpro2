# АНАЛІЗ ЛОГІКИ ТА ЗАДАЧ HELIX SYSTEM

Документ згруповано за сенсом: спочатку дорожня карта, потім опис роботи системи, стан проєкту, якість коду та тестування, ідеї, безпека та платформи.

---

## ЧАСТИНА 1. ДОРОЖНЯ КАРТА

### 🎯 ЗАДАЧА №1 (пріоритет)

**Аналізи в кабінеті гравця та запити в Med-Bay:**
- У кабінеті гравця, де замовляють аналізи, **там же мають з’являтися результати** замовлених аналізів.
- Запити гравців на аналізи мають **падати в окрему вкладку «Запити гравців» у Med-Bay** в адмінці.
- Поки реалізовано **заглушки**: блок «Результати аналізів» у модалці «ЗАМОВИТИ АНАЛІЗИ» (profile) та вкладка «ЗАПИТИ ГРАВЦІВ» у Med-Bay (admin). Доробити: зберігання заявок, відображення результатів у профілі, список запитів у адмінці.

---

### 🎯 ЗАДАЧА №2

**Прописати лідера Олімпа:**
- Нарративне та системне описання ролі обраного лідера Олімпа (після голосування в акті 1).
- Відображення в профілі: бейдж «Лідер Олімпа» або «Лідер: [ім'я]» для обраного лідера; опційно — окрема сторінка/блок з описом повноважень та зв'язків з фракцією.

---

### 🎯 ЗАДАЧА №3

**Прописати взаємозв'язок Треміса (THEMIS) з лідером:**
- Ігрова та лорова логіка взаємодії фракції THEMIS з обраним лідером Олімпа (протоколи, перевірки, конфлікти інтересів).
- **Реалізовано:** квест «Підтвердіть повноваження лідера Іларії» — THEMIS обирає кандидата лідера з OLYMPOS; 3 спроби (невірно — рівень 1, правильно — +1 рівень). **Якщо лідер Олімпа ще не обраний:** у профілі THEMIS показується лише назва та опис квесту, без кнопки та підказки («Лідер ще не обраний» не показуємо).
- Далі (опційно): нарративне описання ролі лідера для THEMIS; відображення «лідер Олімпа» у контексті справедливості; сценарійні зав'язки для ГМ.

---

### ✅ РОБИМО ЗАРАЗ (з Code Review)

- **PHP: валідація/санитизація в адмінці** — перевірка типів, `trim()`, обмеження довжини полів; для виводу в HTML — `htmlspecialchars()` (config: `sanitizeString()`, `h()`).
- **fetch з .catch у profile.html** — у ланцюжках завантаження місій/аналізів додати `.catch()` з показом повідомлення користувачу або fallback, щоб помилка мережі не «зависала» в промісі.
- **Єдині ключі для аудіо** — узгодити на `helix_audio_enabled` (hub + system.js); у hub — міграція зі старих `helix_audio_pref` / `helix_audio_time`.

**Варіанти далі (по можливості):** прибрати/умовно виконувати `console.*` у проді; винести завантаження users у спільний модуль (`api.js`).

### 🎯 ЗАДАЧА (ідеї)

- **Погратись з QR-кодами:** експерименти з розміром, стилем, вмістом (код доступу, посилання на профіль); можливо сканування для швидкого логіну з іншого пристрою.

### ✅ ЗАДАЧА (виконано): Результат тесту «Протокол бункера»

- **Типований результат тесту:** ✅ Реалізовано. Тип профілю (наприклад «Бунтар», «Слухняний», «Лідер», «Системник», «Спостерігач», «Рівноважений» тощо) зберігається в `bunker_test.result_type`, показується в профілі гравця та в Med-Bay адмінки.
- Далі (опційно): працювати з цими типами у сценаріях, місіях, лорі.

---

## 📋 ЗМІСТ (за сенсом)

- **ЧАСТИНА 1.** [Дорожня карта](#частина-1-дорожня-карта) — задачі №1–3, робимо зараз
- **ЧАСТИНА 2.** [Як працює система](#частина-2-як-працює-система) — [правило: квести тільки після початку гри](#правило-квести-тільки-після-початку-гри), [огляд сторінок і системи](#огляд-сторінок-і-логіки-системи), вхід, профіль, реєстрація, PWA, backend
- **ЧАСТИНА 3.** [Стан проєкту](#частина-3-стан-проєкту) — проблеми, не робимо
- **ЧАСТИНА 4.** [Якість коду та тестування](#частина-4-якість-коду-та-тестування) — Code Review, [що зроблено](#що-зроблено-за-code-review-виключено-з-активних-рекомендацій), тест-чекліст
- **ЧАСТИНА 5.** [Ідеї для розвитку](#частина-5-ідеї-для-розвитку) — експорт, ідеї, [прості ідеї](#ідеї-нескладні-швидко-зробити), [інтерфейси та UX](#11a-інтерфейси-та-ux--перевірка-та-ідеї), Чорний Іній
- **ЧАСТИНА 6.** [Безпека та платформи](#частина-6-безпека-та-платформи) — захист даних, Android
- [Перевірка лінків і незавершених розділів](#перевірка-лінків-і-незавершених-розділів)
- [18. Рекомендації після перевірки коду](#18-рекомендації-після-перевірки-коду)
- [19. Перевірка проєкту (аудит)](#19-перевірка-проєкту-аудит)
- [20. Швидкість завантаження та оцінка сайту](#20-швидкість-завантаження-та-оцінка-сайту)

Детальні посилання: [1. Вхід](#1-файли-входу-та-авторизації) · [2. Профіль](#2-файли-профілю-та-кабінету) · [3. Реєстрація](#3-файли-реєстрації-та-персоналу) · [4. PWA](#4-service-worker-та-pwa) · [5. Backend](#5-backend-та-дані) · [6. Проблеми](#6-список-проблем-та-рекомендації) · [8. Не робимо](#8-не-робимо) · [9. Експорт](#9-експорт-профілю-гравця---що-можемо-і-як) · [10–11. Ідеї](#10-ідеї-для-розвитку-група-1) · [11a. Інтерфейси та UX](#11a-інтерфейси-та-ux--перевірка-та-ідеї) · [12. Чорний Іній](#12-чорний-іній-як-механіка--повна-ідея) · [13. Захист](#13-захист-файлу-з-гравцями-і-паролями) · [14. Android](#14-додаток-на-android) · [15. Code Review](#15-огляд-помилок-і-ризиків-code-review) · [16. Тести](#16-протистування-на-баги-тест-чекліст) · [17. Ідеї](#17-додаткові-ідеї-для-розвитку) · [18. Рекомендації після перевірки](#18-рекомендації-після-перевірки-коду)

---

### Перевірка лінків і незавершених розділів

**Лінки:** Додано відсутній заголовок [ЧАСТИНА 5. ІДЕЇ ДЛЯ РОЗВИТКУ](#частина-5-ідеї-для-розвитку) і підрозділ [Ідеї нескладні (швидко зробити)](#ідеї-нескладні-швидко-зробити), щоб посилання зі Змісту вели на існуючі розділи. Інші якорі (частини 1–4, 6, розділи 1–19, підрозділи) відповідають заголовкам.

**Розділи, що ще не доопрацьовані (потребують уваги):**

| Розділ | Стан | Що залишилось |
|--------|------|----------------|
| **ЗАДАЧА №1** (пріоритет) | Поки заглушки | Доробити: зберігання заявок на аналізи, відображення результатів у профілі, список запитів у Med-Bay |
| **ЗАДАЧА №2** | Ідея | Нарратив лідера Олімпа; бейдж «Лідер Олімпа» / «Лідер: [ім'я]» у профілі (опційно) |
| **РОБИМО ЗАРАЗ** | Активно | PHP trim/htmlspecialchars у адмінці; .catch() у profile.html; єдині ключі аудіо (helix_audio_enabled + міграція) |
| **Code Review** | Частково | Екранування з JSON у DOM (personnel, profile, lore); рекомендації по lore, profile XSS, fetch .catch, ключі аудіо |
| **Розділ 18 (рекомендації)** | Довідково | Path traversal upload (опційно); абсолютні шляхи config; перевірка code у get_missions.php (опційно) |
| **Розділ 19 (аудит)** | Довідково | Що перевірити далі: тест-чекліст, результат аналізів у профілі, абсолютні шляхи |

## ЧАСТИНА 2. ЯК ПРАЦЮЄ СИСТЕМА

Опис файлів та логіки: вхід, профіль, реєстрація, PWA, backend, схема авторизації.

### Правило: квести тільки після початку гри

**Усі квести (цілі та місії) з’являються тільки після початку гри — тобто після запуску таймера/акту для відповідної глави.** Якщо не сказано інше (наприклад, окремий сценарій «до гри»), це правило діє для всього контенту «Цілі та місії» в кабінеті гравця.

**Реалізація:**
- Джерело стану гри: **`gamestate.json`** — для кожної глави (`ch1`, `ch2`) є поле **`status`**: `"running"` (таймер/акт запущено) або `"stopped"` (гра не почалась).
- У **`chapter2/profile.html`** при відкритті модалки «Цілі та місії» (`openMissions()`):
  1. Завантажується стан глави: `loadChapterStatus(chapter)` → читає `gamestate.json`.
  2. Якщо **`!isPhaseRunning(chapter)`** (тобто `status !== 'running'`), показується текст: *«Цілі та фракційні завдання з'являться після запуску фази.»* — квести не завантажуються і не відображаються.
  3. Якщо фаза запущена — завантажуються `quests.json`, лідер/голоси, взяті місії, і рендеряться активні та виконані цілі.
- Запуск таймера/акту: в **адмінці → Chapter Control** (`admin.php?view=chapter_control&target=ch2`) ГМ встановлює час і запускає таймер; зміни пишуться в `gamestate.json`.

**Підсумок:** Квести в профілі видно лише коли для глави гравця в `gamestate.json` встановлено `status: "running"`. Інші сторінки (реєстрація, K.I.R.A., бункер, біометрична верифікація) не залежать від таймера, якщо в сценарії не передбачено інше.

---

### Огляд сторінок і логіки системи

| Сторінка / файл | Призначення | Ключова логіка |
|-----------------|-------------|----------------|
| **index.html** | Точка входу | Логін адміна (HELIX2025 → admin.php) або гравця (users.json по access_code → sessionStorage, редирект на chapter1/2 profile). |
| **admin.php** | Адмін-панель | Перевірка пароля, меню: Players, Onboarding, Quests, Med-Bay, Analytics, Audio, Chapter Control, Lore, Instructions. |
| **chapter2/hub.html** | Хаб глави 2 | Вибір аудіо (localStorage), перевірка sessionStorage/code → редирект на profile або логін (users.json, ch2). |
| **chapter2/profile.html** | Кабінет гравця | Код з URL/sessionStorage; get_users.php, ping.php; таймер з timer_sync.js (gamestate.json). **Цілі та місії:** тільки якщо gamestate[chapter].status === 'running', інакше «Цілі з'являться після запуску фази»; quests.json, get_missions/save_missions, акти OLYMPOS/THEMIS, біометрична верифікація, бункер, Med-Bay, плейліст. |
| **chapter2/register.html** | Подача заявки | Крок 1: форма (ім'я, tg, фракція, роль з get_users — вільні ролі). Крок 2: лор-квест «спочатку тест — потім заявка», посилання на test.html?from=register; після K.I.R.A. — кнопка «Відправити заявку» → register_handler.php; при успіху грає музика «4» з audio_config.json. |
| **chapter2/personnel.html** | Список персоналу | get_users.php, фільтр ch2 + фракції (OLYMPOS, ORIGIN, THEMIS, MOIRAI); картки з бейджами ВІЛЬНА/ЗАЙНЯТА/YOU, модалка досьє. |
| **chapter2/test.html** | K.I.R.A. тест | Питання → результат (роль); при from=register зберігає в sessionStorage і редирект на register.html?step=2; опційно save_kira.php. |
| **chapter2/bunker_test.html** | Протокол бункера | Вибір звернення, питання, відповіді → save_bunker_test.php (result_type у users.json); звук «3» з audio_config.json при старті тесту. |
| **chapter2/biometric_scan.html** | Біометрична верифікація (OLYMPOS) | get_biometric_profile.php, biometric_scan_start.php (рівень → 1), етичні питання + загадка, biometric_scan_complete.php (+1 рівень). Доступ тільки для OLYMPOS. |
| **lore.html** | Архів лору | helix_data.json / lore_data.json, фільтри, вивід з esc() для безпеки. |
| **architect/index.html** | YGGDRASIL (декоративна) | Логін і реєстрація неактивні; інформаційна сторінка. |
| **timer_sync.js** | Синхронізація таймера | Запит gamestate.json кожні 10 с; якщо status === 'running', показує зворотний відлік end_time у mission-timer. |
| **gamestate.json** | Стан таймерів глав | ch1/ch2: status (running/stopped), start_time, end_time, duration, acts. Змінюється в Chapter Control. |
| **quests.json** | Цілі для профілю | faction_goals (OLYMPOS, ORIGIN, THEMIS, MOIRAI), personal_goals (по access_code). Використовується в profile після перевірки «фаза running». |
| **audio_config.json** | Аудіо-конфіг | chapters, factions, «3» (звук бункера), «4» (при подачі заявки), background_tracks. Клієнт і адмінка читають/зберігають через save_audio_config.php. |

---

### 1. ФАЙЛИ ВХОДУ ТА АВТОРИЗАЦІЇ

### 1.1 `index.html` (Головна сторінка)
**Призначення:** Точка входу в систему, логін для адмінів та гравців

**Логіка:**
- ✅ Реєструє Service Worker
- ✅ Модальне вікно логіну (`gateway-screen`)
- ✅ **Адмін логін:** `HELIX2025` → POST форма в `admin.php`
- ✅ **Гравець логін:**
  - Завантажує `users.json` (без кешу)
  - Шукає користувача по `access_code`
  - **Фільтр:** `role !== 'GAMEMASTER'` AND `chapter !== 'admin'`
  - **Без збереження в localStorage** — тільки `sessionStorage.setItem('helix_access_code', user.access_code)`
  - **Редирект залежно від chapter:**
    - `ch2` → `chapter2/profile.html?code=...`
    - `ch1` → `chapter1/profile.html?code=...`
    - без `chapter` → `chapter2/profile.html?code=...`

**Поточний стан:** Логіка оновлена, пускає будь-якого гравця на відповідну сторінку глави.

---

### 1.2 `chapter2/hub.html` (Хаб глави 2)
**Призначення:** Центральна сторінка глави 2, логін тільки для гравців ch2

**Логіка:**
- ✅ Екран вибору аудіо (зберігає в `localStorage`: `helix_audio_enabled`; міграція з `helix_audio_pref` / `helix_audio_time`)
- ✅ Перевірка доступу: якщо є `sessionStorage.helix_access_code` → `profile.html?code=...`, інакше → модальне вікно логіну
- ✅ **Адмін логін:** `HELIX2025` → POST форма в `../admin.php`
- ✅ **Гравець логін:**
  - Завантажує `../users.json` (без кешу)
  - **Фільтр:** `role !== 'GAMEMASTER'` AND `chapter !== 'admin'` AND `chapter === 'ch2'`
  - **Без localStorage для auth:** зберігає тільки `sessionStorage.setItem('helix_access_code', user.access_code)`
  - Редирект на `profile.html?code=...`

**Поточний стан:** Тільки гравці глави 2, доступ через sessionStorage + URL.

---

## 2. ФАЙЛИ ПРОФІЛЮ ТА КАБІНЕТУ

### 2.1 `chapter2/profile.html` (Особистий кабінет)
**Призначення:** Профіль гравця, місії, аватарка, QR-код

**Логіка:**
- ✅ **Джерело ідентифікації:** `?code=...` з URL або `sessionStorage.getItem('helix_access_code')`
- ✅ Якщо немає `code` → редирект на `hub.html`
- ✅ Завантажує користувачів через `get_users.php` (з кешем 2 хв для модалки аналізів), знаходить користувача по `access_code`
- ✅ **Таймер:** `timer_sync.js` підключається, читає `gamestate.json` кожні 10 с, показує зворотний відлік у `#mission-timer`, якщо `gamestate[ch2].status === 'running'`
- ✅ **Цілі та місії (квести):** показуються **тільки після початку гри** (див. правило вище). При відкритті модалки «Цілі та місії» (`openMissions()`): завантажується `loadChapterStatus(chapter)` з `gamestate.json`; якщо **`!isPhaseRunning(chapter)`** — показується текст «Цілі та фракційні завдання з'являться після запуску фази», квести не завантажуються; якщо фаза `running` — завантажуються `quests.json`, лідер/голоси, взяті місії, і рендеряться активні/виконані цілі
- ✅ **Цілі з сервера:** `quests.json` (lazy при відкритті модалки): `faction_goals` (по фракції), `personal_goals` (по `access_code`); зберігає в `FACTION_GOALS`, `PERSONAL_GOALS`
- ✅ **Взяті місії:** `get_missions.php` / `save_missions.php` (`missions_state.json`), макс. 2
- ✅ **Акт 1 OLYMPOS:** біометрична верифікація (термінал → рівень 1, після тесту +1); «Повний доступ» → `act1_full_access.php`, рівень 1; «Обрати лідера» → голосування, при 2/3 — лідер, +1 рівень усім OLYMPOS
- ✅ **Акт 1 THEMIS:** «Підтвердіть повноваження лідера» — якщо лідер ще не обраний: лише назва та опис; після обраного лідера: dropdown, 3 спроби (невірно — рівень 1; правильно — +1)
- ✅ **Замовити аналізи:** модалка, POST у `submit_analysis_request.php` → Med-Bay
- ✅ Рендер профілю: код, ім'я, роль, LVL, історія, QR-код, аватарка (`../uploads/${code}.jpg`)

**Поточний стан:** Квести тільки після запуску фази; акт 1 OLYMPOS (біометрична, повний доступ, лідер) та THEMIS реалізовано.

---

## 3. ФАЙЛИ РЕЄСТРАЦІЇ ТА ПЕРСОНАЛУ

### 3.1 `chapter2/register.html` (Форма реєстрації)
**Призначення:** Подача заявки на роль

**Логіка:**
- ✅ При завантаженні завантажує `../users.json` (без кешу) → змінна `dbUsers`
- ✅ При виборі фракції (`updateRoles`):
  - Завантажує свіжі дані з `../users.json`
  - **Фільтр:** виключає `role === 'GAMEMASTER'`, `chapter === 'admin'`, `access_code === 'HELIX2025'`; потім `chapter === 'ch2'`, `faction === selected`, **вільна** = `booking_status === 'free'` або відсутній `booking_status`
  - Показує список вільних ролей
- ✅ Відправка: POST на `../register_handler.php`, потім екран "RECEIVED"

**Поточний стан:** GAMEMASTER не вважається роллю для реєстрації; вільна = free або без статусу.

---

### 3.2 `chapter2/personnel.html` (Список ролей)
**Призначення:** Відображення персоналу по фракціях (глава 2)

**Логіка:**
- ✅ **Поточний користувач:** `?code=...` з URL або `sessionStorage.getItem('helix_access_code')`
- ✅ Завантаження: `loadUsers()` — спочатку `../users.json`, fallback на `${origin}/users.json`
- ✅ **Фільтр:** виключає GAMEMASTER, admin, HELIX2025; лишає `chapter === 'ch2'` і валідну фракцію (OLYMPOS, ORIGIN, THEMIS)
- ✅ **Статус ролі (без плутанини з GAMEMASTER як роллю):**
  - **ВІЛЬНА:** `booking_status === 'free'` або немає `booking_status`
  - **ЗАЙНЯТА:** є `booking_status` і не `free`
  - **YOU:** поточний користувач за `access_code`
- ✅ Картки: аватарка з placeholder, бейджі "YOU" / "ВІЛЬНА" / "ЗАЙНЯТА", модальне вікно з деталями

**Поточний стан:** GAMEMASTER — окремо (не роль для сітки); роль або вільна, або зайнята.

---

## 4. SERVICE WORKER ТА PWA

### 4.1 `sw.js` (Service Worker)
**Призначення:** Кешування для офлайн, автооновлення кешу

**Логіка:**
- ✅ **Install:** кешує файли з `ASSETS_TO_CACHE` (включаючи `users.json`)
- ✅ **Activate:** очищає старі кеші, перевірка автооновлення о 3:00
- ✅ **Fetch:**
  - Навігація: network-first → cache → `offline.html`
  - **JSON-дані** (`users.json`, `gamestate.json`, `quests.json`, `helix_data.json`): network-first, `cache: 'no-store'`, fallback на кеш при помилці мережі
  - Інші GET: cache-first → network → `offline.html`
- ✅ Автооновлення кешу о 3:00 (якщо не оновлювали сьогодні)

**Поточний стан:** Дані JSON не кешуються після відповіді, тільки fallback при офлайн.

---

## 5. BACKEND ТА ДАНІ

### 5.1 `admin.php`
**Призначення:** Адмін-панель (перевірка пароля на сервері).

### 5.2 `register_handler.php`
**Призначення:** Обробка заявок на реєстрацію (POST з форми).

### 5.3 `upload.php`
**Призначення:** Завантаження аватарок по `access_code`.

### 5.4 `quests.json`
**Призначення:** Цілі для профілю (фракційні та персональні).

**Структура:**
- `faction_goals`: об'єкт, ключі — фракції (OLYMPOS, ORIGIN, THEMIS), значення — масиви `{ id, title, text, action? }`
- `personal_goals`: об'єкт, ключі — `access_code`, значення — масиви `{ id, title, text }`

**Поточний стан:** Використовується в `profile.html` через `loadGoals()`. В адмінці — перегляд і редагування в розділі **ЦІЛІ / QUESTS** (`admin.php?view=quests`). Акт 1: OLYMPOS — `action: "full_access"`, `"vote_leader"`; THEMIS — `action: "confirm_leader"` (F_THEMIS_LEADER_CONFIRM).

### 5.5 Акт 1: голосування та THEMIS
- **`get_leader_votes.php`** — список OLYMPOS, голоси, обраний лідер; при `?code=...` для гравця THEMIS також повертає `themis_attempts_used`, `themis_attempts_left`, `themis_confirmed` з `themis_leader_confirm.json`.
- **`submit_leader_vote.php`** — збереження голосу в `act1_leader_votes.json`; при 2/3 голосів за одного — лідер обрано, +1 рівень усім OLYMPOS.
- **`submit_themis_leader_confirm.php`** — THEMIS підтверджує кандидата лідера; 3 спроби: правильно — +1 рівень, `themis_confirmed`; невірно — рівень 1. Стан у `themis_leader_confirm.json`.

---

## ЧАСТИНА 3. СТАН ПРОЄКТУ

Проблеми, що залишилися, вирішені пункти та виключене з робіт.

### 6. СПИСОК ПРОБЛЕМ ТА РЕКОМЕНДАЦІЇ

### Вирішено

- **Пам'ять між сесіями:** політика реалізована — опційне збереження коду в sessionStorage з TTL (`session_store.js`, TTL 24 год). Використовується в index, hub, architect при логіні; profile, personnel, test читають код з URL або store.

### 🟡 СЕРЕДНІ (залишилися)

10. **Діагностика** — У personnel.html логи в консолі; для прод можна зменшити або виносити в окремий лог.

11. **Посилання з профілю** — При навігації на personnel/terminal передавати `?code=...` для стабільного "YOU".

## 8. НЕ РОБИМО

- ~~Офлайн-режим: повідомлення + «Повторити».~~ Прибрано.
- ~~Темна/світла тема.~~
- ~~**Валькірії в адмінці:** окремий фільтр «полеглі» (інфіковані) в Med-Bay/Personnel для швидкого вилучення.~~ Не робимо.
- **Ragnarök-таймер:** глобальний «часу до події» окремо від таймера глави — не робимо.

---

## ЧАСТИНА 5. ІДЕЇ ДЛЯ РОЗВИТКУ

Експорт, ідеї для нових фіч, Чорний Іній та інше.

### 9. ЕКСПОРТ ПРОФІЛЮ ГРАВЦЯ — ЩО МОЖЕМО І ЯК

**Мета:** Кнопка в профілі або адмінці — експорт персонажа (біо, здібності, K.I.R.A., роль, фракція) у PDF або текст.

**Що зможемо:**
- **Текст (.txt):** Звичайний файл: ім’я, код, роль, фракція, біо (`history`), здібності (`abilities`), результат K.I.R.A. (`kira_result.type`), опційно мед-статус. Без сервера: на клієнті зібрати рядок з даних, які вже є в профілі, і зберегти через `Blob` + `URL.createObjectURL` + `<a download>`.
- **PDF:** Гарний односторінковий «досьє» з логотипом/стилем. Варіанти:
  1. **Клієнт (браузер):** Бібліотека типу **jsPDF** або **pdf-lib**: на стороні гравця зібрати текст + опційно зображення (аватар), згенерувати PDF і скачати. Плюс: не навантажує сервер. Мінус: потрібно підключити скрипт, стилізація обмежена.
  2. **Сервер (PHP):** Скрипт `export_profile.php?code=XXX` (доступ тільки для власника коду або адміна): читає `users.json`, знаходить юзера, формує текст або викликає **TCPDF** / **FPDF** для генерації PDF і віддає `Content-Disposition: attachment`. Плюс: єдине місце логіки, можна додати водяний знак «HELIX», шаблон. Мінус: потрібна бібліотека на сервері.

**Рекомендація:** Спочатку реалізувати **експорт у .txt** у профілі (кнопка «Експорт досьє» → збір полів з поточного юзера → download). Для PDF — або клієнтський jsPDF (швидко), або серверний PHP (повний контроль шаблону).

---

## ЧАСТИНА 5. ІДЕЇ ДЛЯ РОЗВИТКУ

## 10. ІДЕЇ ДЛЯ РОЗВИТКУ (група 1)

- **Медичний модуль:** Повноцінний інтерфейс для гравців/ГМ: історія хвороб, призначення аналізів, відмітки по мед-карті, інтеграція з Med-Bay.
- **Глава 1:** Окремий hub і профіль для ch1 (Arctic).
- **Сповіщення:** Push (PWA) для оголошень або дедлайнів.
- **Лор/Термінал:** Інтеграція lore_data.json у сторінки глави; термінал доступу як окрема функція.
- **Рол-плей чат/листування:** Чат по фракціях або листи між персонажами (JSON або БД).
- **Аналітика для ГМ:** ✅ Сторінка в адмінці: хто заходив, коли, K.I.R.A., Протокол бункера, взяті місії (агреговано, без PII). Реалізовано: `admin_modules/analytics.php`, `ping.php` (оновлення `last_active` при заході в кабінет), пункт меню ANALYTICS.
- **Інтеграція з Telegram:** Login Widget або бот; коди/нагадування в бот.
- **Рунічна навігація на architect:** Посилання на глави з YGGDRASIL.
- **МІМІР як API:** Концепт «платіж знанням» — обмін місіями/доступом за умов.

---

## 11. ІДЕЇ ДЛЯ РОЗВИТКУ (група 2)

1. **Експорт профілю:** Кнопка в профілі/адмінці — персонаж у PDF або .txt (див. розділ 9).
2. **Двофакторний вхід для ГМ:** Додатковий крок авторизації для admin.php (код з пошти).
3. **Календар подій:** В адмінці — дати/події глави; на hub/profile — «найближча подія» (з gamestate або окремого JSON).
4. **Шаблони листів:** ГМ вибирає шаблон (наприклад «Запрошення на місію») і підставляє ім’я/роль; відправка через Telegram або копія в буфер.
5. **Історія змін профілю:** Лог змін полів гравця (хто, коли, що змінив) — для аудиту та відновлення.
6. **Інвайт-коди:** Генерація одноразових посилань/кодів для запрошення гравців (зберігати використані в users або окремому файлі).
7. **Міні-карта/локації:** Схема бункера або секторів з позначками «де що відбувається» (статична картинка + зони-посилання або popup з описом).

### Ідеї нескладні (швидко зробити)

- **Кнопка «Скопіювати код»** у профілі — копіювання `access_code` у буфер обміну одним кліком.
- **Підсвітка поточного розділу** в навігації адмінки (вже є `.active`; переконатися, що він застосований до всіх пунктів).
- **Підказка (title)** на кнопках профілю — короткий опис при наведенні («Відкрити досьє», «Цілі та місії» тощо).
- **Порожній стан у списку місій** — один стиль повідомлення («Цілі з'являться після запуску фази» / «Немає взятих місій») з вирівнюванням по центру.
- **Час останньої активності** у Analytics — форматування «X хв тому» / «сьогодні о HH:MM» для зручності ГМ.
- **Обмеження розміру аватарки** на клієнті — перевірка `file.size` перед відправкою (наприклад, макс. 2 МБ) і повідомлення користувачу.

---

### 11a. ІНТЕРФЕЙСИ ТА UX — ПЕРЕВІРКА ТА ІДЕЇ

**Зроблено:**
- **Personnel (список персонажів):** модалка біо закривається при кліку поза блоком (backdrop); кнопка «×» має `aria-label="Закрити"`; клік по контенту (.paper) не закриває (`stopPropagation`).

**Ідеї по інтерфейсах:**
1. **Модалки — клік поза / Escape:** ✅ Зроблено. У профілі модалки «Досьє», «Цілі та місії», «Замовити аналізи», плейліст — закриття при кліку по затемненню та по Escape (консистентно з personnel).
2. **Єдиний стиль закриття:** У всіх модалок однакова позиція кнопки закриття (наприклад, верхній правий кут), однаковий курсор і hover.
3. **Фокус і доступність:** При відкритті модалки переносити фокус на перший інтерактивний елемент або на кнопку закриття; при закритті повертати фокус на елемент, що відкрив модалку (для клавіатурної навігації).
4. **Personnel:** логі в консолі обгорнути в `if (window.DEBUG)` або прибрати для прод (вже в пункті 6).
5. **Реєстрація:** кроки 1→2→3 мають чіткі заголовки та підказку «назад» на кроці 2 (повернутись до редагування форми без втрати чернетки).
6. **Hub/Profile:** кнопки однакової важливості — однаковий розмір/контраст; головна дія (наприклад, «ОСОБИСТИЙ КАБІНЕТ») виділена візуально.
7. **Порожні стани:** Якщо список цілей порожній або «Цілі з'являться після запуску фази» — переконатися, що текст вирівняний по центру і не виглядає як помилка.

---

## 12. ЧОРНИЙ ІНІЙ ЯК МЕХАНІКА — ПОВНА ІДЕЯ

**Концепт:** Ігрова механіка «зараження» Чорним Інієм (Black Goo) — нарративна та системна частина YGGDRASIL.

**Відображення:**
- **Профіль гравця:** Окремий індикатор «Чорний Іній» (рівень 0–100% або ступені: чистий / контакт / зараження / контроль). Візуал: холодна синя/чорна шкала, рунічний символ.
- **Мед-карта / Med-Bay:** Поле `stats.black_goo` (число або ступінь). В адмінці Med-Bay — колонка «Чорний Іній» для фільтрації та призначення «вилучення» (Валькірії).
- **Статус у personnel:** Опційно — бейдж «ІНФІКОВАНО» або «КОНТАКТ» замість/поруч із мед-статусом.

**Вплив на доступ:**
- **Нижній поріг (наприклад &lt; 30%):** Повний доступ до профілю, hub, місій.
- **Середній (30–70%):** Обмеження: частина розділів «заблокована системою» (наприклад, окремі документи лору, або завдання «здати аналізи» обов’язкове перед наступною місією).
- **Високий (70%+):** Персонаж позначений як «полеглий» — Валькірії рекомендують вилучення; доступ до профілю лише для перегляду + кнопка «Запит на очищення» (ГМ вирішує ігрово).
- **Критичний (100% або «контроль»):** Персонаж переходить у стан «частина ORIGIN» — або блокується гравець, або відкривається спеціальний сценарій (на керування ГМ).

**Джерела зміни рівня:**
- ГМ вручну в Med-Bay / Players (поле `black_goo`).
- Ігрові події: «контакт з джерелом», «виконання місії в зоні ризику» — через адмінку або майбутній модуль подій.
- «Замовити аналізи» — як опція зниження рівня (обмін «знання/дані» на очищення — тема МІМІР).

**Технічно:** Розширити `users[].stats`: додати `black_goo: 0` (0–100) або `black_goo_stage: "clean"|"contact"|"infected"|"control"`. Профіль і Med-Bay читають це поле; логіку обмежень реалізувати в JS (приховування блоків) та/або в PHP (фільтр контенту за рівнем).

---

## ЧАСТИНА 6. БЕЗПЕКА ТА ПЛАТФОРМИ

Захист даних, доступ до users.json, PWA та Android.

### 13. ЗАХИСТ ФАЙЛУ З ГРАВЦЯМИ І ПАРОЛЯМИ

**Реалізовано:**
- **Прямий доступ до `users.json` заборонено** в `.htaccess` (`<Files "users.json">` / `Require all denied`).
- **API-проксі `get_users.php`:** читає `users.json` на сервері і віддає JSON; фронтенд (index, hub, profile, personnel, register, architect/, auth.js) використовує `get_users.php` замість `users.json`. SW кешує `get_users.php` замість `users.json`.
- **missions_state.json** — як і раніше заборонений прямий доступ (доступ тільки через get_missions.php / save_missions.php).

**Далі (опційно):**

1. **Винести файл за межі document root:** Зберегти `users.json` у каталозі поза коренем сайту (наприклад, `../private/users.json`) і оновити шлях у `get_users.php` та PHP-скриптах. Тоді прямий URL до файлу взагалі не існує.

2. **Nginx:** Аналогічно: `location = /users.json { deny all; }`. API — через окремі location (php).

3. **Додатково:** Не зберігати паролі в открытому вигляді; якщо з’являться паролі — тільки хеш. Зараз доступ по коду (`access_code`) — код грає роль «секретного токена».

---

## 📝 ПОТОЧНА СХЕМА АВТОРИЗАЦІЇ

| Дія              | Де зберігається          | Перехід / використання                    |
|------------------|--------------------------|-------------------------------------------|
| Логін з index    | sessionStorage (helixSessionStore + TTL) | Редирект на `chapterX/profile.html?code=...` |
| Логін з hub      | sessionStorage (helixSessionStore + TTL) | Редирект на `profile.html?code=...`       |
| Відкриття профілю| —                        | Читання `?code=...` або helixSessionStore.getStoredCode() |
| Взяті місії      | Сервер: `missions_state.json` (через get_missions.php / save_missions.php); клієнт: sessionStorage як кеш |
| Аудіо (hub)      | localStorage             | `helix_audio_enabled` (міграція зі старих ключів) |

**localStorage не використовується для auth і профілю.**  
**Місії:** Реалізовано збереження на сервері — профіль завантажує/зберігає через API; кросс-пристрої підтримуються.  
**GAMEMASTER не вважається роллю в сітці ролей — тільки вільна/зайнята.**

**Політика «пам'яті» між сесіями (реалізовано):** Опційне збереження коду в **sessionStorage** з TTL. Файл `session_store.js`: `helixSessionStore.getStoredCode()` / `helixSessionStore.setStoredCode(code, ttlMs)`. Ключі: `helix_access_code`, `helix_access_code_ttl` (timestamp закінчення). TTL за замовчуванням — 24 год. Після закінчення TTL код очищається при наступному читанні. Використовується в index, hub, architect (при логіні) та в profile, personnel, test (при читанні коду).

---

### 14. ДОДАТОК НА ANDROID

**Чи можна зробити сайт як додаток на Android?** Так.

- **PWA (вже є):** У проєкті є `manifest.json` та Service Worker (`sw.js`) — користувач може в Chrome (Android) відкрити сайт → меню → «Додати на головний екран» / «Встановити додаток». Сайт відкриватиметься в повноекранному режимі без адресного рядка.
- **Повноцінний APK:** Для публікації в Google Play можна зібрати обгортку: **TWA (Trusted Web Activity)** через Bubblewrap або **Capacitor** — упакувати той самий веб-сайт у APK. Дані (логін, кеш) залишаються в WebView; оновлення контенту — через оновлення сайту на сервері.

---

## ЧАСТИНА 4. ЯКІСТЬ КОДУ ТА ТЕСТУВАННЯ

Тут зібрано Code Review та тест-чекліст.

### 15. ОГЛЯД ПОМИЛОК І РИЗИКІВ (CODE REVIEW)

Нижче — виявлені при аналізі коду проблеми, ризики та рекомендації.

### 🔴 Критичні / безпека

1. **XSS при виводі даних з JSON у DOM:** ✅ **Частково вирішено.** Екранування виводу: у `personnel.html` — `esc(u.name)`, `esc(u.role)` при формуванні карток; у `profile.html` — `esc(goal.title)`, `esc(goal.text)` у модалці місій; у `lore.html` — `esc(item.title)`, `esc(item.text)` тощо. В адмінці (`players.php`) — `strip_tags` + `trim` для полів при збереженні. Рекомендація: продовжувати використовувати `esc()` для будь-якого контенту з JSON у `innerHTML`.

2. **PHP: відсутня явна валідація/санитизація вводу:** У `admin_modules/players.php`, `quests.php`, `onboarding.php`, `lore.php` тощо дані з `$_POST` часто пишуться в JSON без перевірки типів і довжини. **Робимо:** перевірка типів, `trim()`, обмеження довжини полів; для виводу в HTML — `htmlspecialchars()` (у config — `sanitizeString()`, `h()`).

3. **upload.php:** ✅ **Зроблено.** Перевірка коду по `users.json` перед збереженням; тільки image/jpeg, image/png (розширення + MIME через finfo); профіль — `accept="image/jpeg,image/png"`.
  
### 🟡 Середні / якість коду

4. **console.log / console.warn / console.error у прод-коді:** Логи є в `index.html`, `chapter2/hub.html`, `chapter2/personnel.html`, `chapter2/register.html`, `chapter2/profile.html`, `chapter2/test.html`, `sw.js`, `auth.js`, `timer_sync.js`, `system.js`. Для продакшену краще прибрати або обгортати в умову (наприклад, `if (window.DEBUG)`).

5. **fetch без .catch:** У `chapter2/profile.html` є ланцюжки `.then().then()` без `.catch()` (завантаження місій, аналізів). При помилці мережі помилка може «зависнути» у промісі. **Робимо:** додати `.catch()` з показом повідомлення користувачу або fallback.

6. **lore.html:** `container.innerHTML = data.map(...)` — якщо в `lore_data.json` потрапить HTML у полях `title`/`text`, можливий XSS. **Рекомендація:** екранувати значення при виводі або дозволити лише довірений контент з адмінки.

7. **profile.html — модальні вікна місій:** `wrap.innerHTML = '<div class="mission-title">' + (goal.title || '') + ...` — якщо в `goal.title`/`goal.text` є HTML, буде XSS. **Рекомендація:** використовувати `textContent` або екранування.

### 🟢 Низькі / дрібниці

8. **Різні ключі для аудіо:** У hub було `helix_audio_pref` / `helix_audio_time`, у `system.js` — `helix_audio_enabled`. **Робимо:** один ключ `helix_audio_enabled`, у hub — міграція зі старих ключів.

9. **Дублювання логіки завантаження users.json:** ✅ **Спрощено:** index і hub використовують `auth.js` → `loginPlayer()`; profile має одну `loadUsers()` з кешем 2 хв і використовує її в loadProfile, модалках аналізів і місій; `session_store.js` має `getAccessCode()` (URL → store → sessionStorage), його використовують profile, personnel, hub.

10. **PHP: можливі undefined index:** У деяких модулях використовується `$_POST['field']` без перевірки `isset()` — при відсутньому полі PHP може видавати notice. **Рекомендація:** використовувати `$_POST['field'] ?? ''` або `isset($_POST['field'])`.

11. **SW: кешування users.json:** У `sw.js` при fetch для JSON-файлів відповідь клонується і кешується при успіху. Якщо потрібна завжди свіжа дані з мережі, варто переглянути політику кешу для `users.json` (наприклад, network-only для даних, а не cache fallback).

### Що зроблено за Code Review (виключено з активних рекомендацій)

- **upload.php:** перевірка `access_code` по `users.json`; обмеження типів файлу (image/jpeg, image/png + MIME).
- **Аналітика для ГМ:** сторінка ANALYTICS, ping/last_active, агрегати без PII (розділ 10).
- **Модалки профілю:** клік по затемненню та Escape для «Досьє», «Цілі та місії», «Замовити аналізи», плейліст (розділ 11a).
- **Протокол бункера:** типований результат (result_type), відображення в профілі та Med-Bay.

### Підсумок (активні рекомендації)

- **Робимо:** PHP trim/довжина/htmlspecialchars у адмінці; .catch() у profile.html для fetch; єдині ключі аудіо (helix_audio_enabled + міграція).
- **Обов'язково:** екранування виводу з JSON у DOM (personnel, profile, lore).
- **По можливості:** прибрати/умовно console.*, спільний модуль для users.json, isset/?? у PHP.

---

### 16. ПРОТИСТУВАННЯ НА БАГИ (ТЕСТ-ЧЕКЛІСТ)

Нижче — сценарії перевірки та виявлені/потенційні баги.

### Тест-чекліст (ручне протистування)

1. **Логін:** index → код гравця ch2 → редирект на profile; код адміна → admin.php. Hub → код ch2 → profile. Architect → код гравця → редірект на chapter2/profile.
2. **Профіль:** без code → редірект на hub. З code — завантаження users, відображення LVL, цілі з quests.json. Відкрити модалку місій — список цілей (фракція + особисті). OLYMPOS: задача «Повний доступ» → alert, рівень стає 1; задача «Обрати лідера» — випадаючий список OLYMPOS, голосування. THEMIS: якщо лідер Олімпа ще не обраний — квест «Підтвердіть повноваження лідера Іларії» показує лише назву та опис (без кнопки/підказки); після обраного лідера — dropdown OLYMPOS, «Підтвердити», 3 спроби (невірно — рівень 1, правильно — +1 рівень).
3. **Замовити аналізи:** відкрити модалку — список об'єктів (з кешу або fetch). Обрати, ЗАМОВИТИ → запит у submit_analysis_request.php; Med-Bay → вкладка «Запити гравців» — з'являється рядок.
4. **Med-Bay (адмін):** Моніторинг — зміна PSY/статус; Запити гравців — таблиця без помилок при порожньому або пошкодженому analysis_requests.json.
5. **Players / Onboarding:** зміна рівня допуску (1–5), збереження; таблиця гравців показує колонку «Рівень допуску».
6. **Голосування за лідера:** тільки OLYMPOS; при 2/3 голосів за одного — лідер обрано, у всіх OLYMPOS +1 рівень; повторне голосування заблоковане.
7. **THEMIS підтвердження лідера:** якщо лідер Олімпа ще не обраний — у THEMIS квест показує лише назву та опис. Після обраного лідера — dropdown, кнопка «Підтвердити», 3 спроби; невірно — рівень 1, правильно — +1 рівень; після вичерпання спроб — «Спроби вичерпано».

### Виявлені / потенційні баги

1. **Обрати лідера — порожній список:** Якщо `get_leader_votes.php` не відповідає або повертає помилку, у OLYMPOS у блоці «Обрати лідера» буде порожній випадаючий список; кнопка «Голосувати» залишиться активною → при відправці сервер поверне помилку. **Рекомендація:** при помилці fetch показувати «Не вдалося завантажити список» і вимикати кнопку.
2. **Один гравець OLYMPOS:** Поріг 2/3 = ceil(1) = 1. Один голос за себе одразу дає лідера. Для ігрологу може бути небажано. **Рекомендація:** опційно встановити мінімум 2 гравці OLYMPOS для активації голосування або окремо описати в мануалі.
3. **act1_full_access повторний виклик:** На бекенді не перевіряється `act1_full_access_used` — повторний POST просто знову ставить level = 1. Не критично, але можна додати перевірку і повертати «Вже виконано».
4. **analysis_requests.json пошкоджено:** Якщо файл не валідний JSON або не масив, Med-Bay (medbay.php) отримує не масив і foreach може дати помилку. **Рекомендація:** у medbay.php перевіряти `is_array($analysisRequests)` і при невалідному виводити порожню таблицю або повідомлення.
5. **Модалка місій — затримка списку:** При відкритті модалки `buildMissions(u)` асинхронний; якщо get_leader_votes повільний, список цілей з'являється з затримкою. Користувач може встигнути натиснути кнопку до появи dropdown. **Рекомендація:** показувати «Завантаження…» поки buildMissions не завершиться.
6. **personnel.html / register:** Завантаження users через get_users.php — при 404 або CORS помилка в консолі; сторінка може залишитися з «— завантаження —». Перевірити fallback-повідомлення.

### Регресія після змін

- Після додавання нових полів у users.json (level, act1_full_access_used) перевірити, що старі записи без цих полів не ламають профіль (значення за замовчуванням).
- Після змін у quests.json (нова ціль OLYMPOS) перевірити, що ORIGIN/THEMIS не бачать чужих цілей.
- Після повного сбросу перевірити, що act1_leader_votes.json та themis_leader_confirm.json скинуті, місії очищені.

---

### 17. ДОДАТКОВІ ІДЕЇ ДЛЯ РОЗВИТКУ

- **Акт 1 для ORIGIN та THEMIS:** Аналогічні сценарії «першої задачі» та механіки голосування/вибору для інших фракцій (наприклад, вибір представника, екстрене рішення).
- **Результати аналізів у профілі:** Після того як адмін у Med-Bay відмітить запит як «Виконано», у кабінеті гравця в блоці «Результати аналізів» показувати відповідний результат (текст або статус).
- **Статус запиту в Med-Bay:** Кнопки «Виконано» / «Скасовано» біля кожного запиту в таблиці «Запити гравців» з збереженням у analysis_requests.json (поле status).
- **Лідер OLYMPOS у профілі:** Відображати бейдж «Лідер Олімпа» або «Лідер: [ім'я]» для обраного лідера (зберігати в users або окремому джерелі після голосування).
- **Історія змін рівня:** Лог подій «хто коли змінив рівень» (акт 1, голосування, ручна зміна в адмінці) для аудиту.
- **Мінімум голосів для лідера:** Опція «лідер обраний лише якщо проголосувало не менше N гравців» (наприклад, 50% від OLYMPOS), щоб уникнути вибору одним гравцем.
- **Календар подій (акти):** Розділ у адмінці або gamestate: дати початку/кінця актів, автоматичне відкриття задач за розкладом.
- **Експорт профілю (.txt):** Кнопка в профілі «Експорт досьє» — збір полів (ім'я, код, роль, фракція, біо, здібності, LVL) у текстовий файл і download (клієнтсько, без сервера).

---

## 18. РЕКОМЕНДАЦІЇ ПІСЛЯ ПЕРЕВІРКИ КОДУ

Нижче — підсумок перегляду коду проєкту та пріоритетні рекомендації.

### Безпека

| Пріоритет | Що | Де | Рекомендація |
|-----------|-----|-----|--------------|
| ~~Високий~~ | ~~Завантаження аватарки під чужим кодом~~ | `upload.php` | ✅ **Зроблено:** перевірка коду по `users.json`; тільки image/jpeg, image/png (MIME + розширення). |
| **Високий** | Path traversal у імені файлу | `upload.php` | Санитизувати `$code` для імені файлу: лише буквено-цифрові, дефіс, підкреслення (опційно). |
| **Середній** | Читання місій будь-якого гравця | `get_missions.php` | За бажанням — перевіряти, що `code` належить існуючому гравцю в `users.json` (зараз будь-хто з кодом може читати місії). |
| **Середній** | Шляхи до JSON у адмінці | `admin_modules/config.php` | Використовувати абсолютні шляхи: `define('USERS_FILE', dirname(__DIR__) . '/users.json')`, аналогічно для `QUESTS_FILE`, `LORE_FILE`, `GAMESTATE_FILE`) — щоб не залежати від поточної робочої директорії. |

### Якість коду та надійність

| Що | Де | Рекомендація |
|-----|-----|--------------|
| Санитизація вводу в lore | `admin_modules/lore.php` | Використовувати `sanitizeString()` для полів `title`, `text`, `display_id`, `category`, `level` при збереженні (як у players.php). |
| Повторний виклик «Повний доступ» | `act1_full_access.php` | Перевіряти `act1_full_access_used` у гравця; якщо вже true — повертати `{ ok: false, error: 'Вже виконано' }` замість повторного змінення рівня. |
| Помилки fetch у модалці місій | `chapter2/profile.html` — `openMissions()` | У `.catch()` показувати користувачу «Не вдалося завантажити список» замість порожнього блоку; вимикати кнопку «Голосувати», якщо список OLYMPOS не завантажився. |
| Захист від пошкодженого JSON | `admin_modules/medbay.php` | Перевірка `is_array($analysisRequests)` вже є; переконатися, що при порожньому/невалідному файлі не виникає PHP notice (окремий `@` або try/catch при `file_get_contents`). |

### Консистентність та підтримка

| Що | Рекомендація |
|-----|--------------|
| Дублювання config | У проєкті є `admin_modules/config.php` і `admin_modules/admin_modules.php` з власними `getJson`/`USERS_FILE`. Використовувати один джерело правди (config.php); admin_modules.php не підключати або видалити дублікати. |
| Ключі відповіді API | Частина скриптів повертає `success`, частина — `ok`. Для нових ендпоінтів узгодити один формат (наприклад, завжди `{ ok: boolean, error?: string }`). |
| Логування в проді | `console.log` у index.html, hub, profile тощо — для продакшену обгортати в `if (window.DEBUG)` або прибирати. |

### Продуктивність

| Що | Статус / рекомендація |
|-----|------------------------|
| Паралельне завантаження кабінету | Зроблено: `get_users.php` і `quests.json` завантажуються через `Promise.all`. |
| Кеш users у get_users.php | Зроблено: серверний кеш 60 с + клієнтський `Cache-Control: private, max-age=25` + ETag для 304. |
| Preload get_users | Зроблено: profile — preload у `<head>`; index/hub — preload при відкритті модалки логіну. |
| Шрифти | Preconnect до fonts.googleapis.com / gstatic; завантаження шрифтів асинхронне (`media="print"` onload) на profile/hub. |
| Кеш quests у браузері | За бажанням: для `quests.json` дозволити кеш на 1–2 хв (зараз `cache: 'no-store'`), якщо цілі змінюються рідко. |

### Вже добре реалізовано

- Екранування виводу: `esc()` у profile (місії), personnel (картки), lore (картки); `h()` у адмінці (players, тощо).
- Валідація та санитизація в адмінці: players.php використовує `sanitizeString()` та `h()`.
- Перевірка прав у submit_analysis_request.php та submit_leader_vote.php (фракція, chapter, рівень).
- Захист прямих URL до `users.json` та `missions_state.json` у `.htaccess`.
- Обробка помилок у index.html (processGateway) — try/catch і повідомлення користувачу.
- upload.php: перевірка коду по users.json; тільки JPEG/PNG (MIME + розширення).

---

## 19. ПЕРЕВІРКА ПРОЄКТУ (АУДИТ)

Короткий огляд структури та стану після перевірки.

### Структура

- **Корінь:** index.html, admin.php, get_users.php, get_missions.php, save_missions.php, submit_analysis_request.php, act1_full_access.php, get_leader_votes.php, submit_leader_vote.php, submit_themis_leader_confirm.php, quests.json, users.json, gamestate.json, helix_data.json, applications.json, manifest.json, sw.js, style.css, offline.html, upload.php, register_handler.php, save_kira.php, session_store.js, timer_sync.js, system.js, a11y.js, declaration.html, lore.html.
- **chapter2:** hub.html, profile.html, personnel.html, register.html, test.html.
- **architect:** index.html, register.html (окремо від глав).
- **admin_modules:** config.php, instructions.php, players.php, applications.php, lore.php, medbay.php, quests.php, onboarding.php, chapter_control.php, audio.php, admin_modules.php.
- **Файли, що створюються під час роботи:** analysis_requests.json, act1_leader_votes.json, themis_leader_confirm.json, missions_state.json, .get_users_cache.json.

### Виправлено під час аудиту

- **Повний сброс (admin.php):** додано скидання `themis_leader_confirm.json` (attempts + confirmed у порожні масиви), щоб після «ПОВНИЙ СБРОС: РОЛІ + ЗАВДАННЯ» THEMIS-квест також скидався.

### Зауваження

- **Файл `GAMESTATE_FILE` у корені:** існує файл без розширення з ім'ям `GAMESTATE_FILE`, у ньому — JSON у форматі gamestate (ch1). Це, ймовірно, бекап або помилково збережений файл. Робочий файл — `gamestate.json`. Рекомендація: перейменувати в `gamestate_backup.json` або видалити, щоб не плутати з константою `GAMESTATE_FILE` у config.
- **lore_data.json і helix_data.json:** у проєкті є обидва; лор у адмінці та lore.html використовують `helix_data.json` (LORE_FILE у config). `lore_data.json` кешується в sw.js — можливо, залишок старої назви; можна залишити для сумісності або прибрати з кешу, якщо не використовується.
- **Шляхи в config.php:** USERS_FILE, LORE_FILE, QUESTS_FILE, GAMESTATE_FILE задані відносними шляхами; при виклику з admin.php робоча директорія — корінь, тому все працює. Для надійності можна перейти на абсолютні шляхи (dirname(__DIR__) у кожному модулі або в config), як у розділі 18.

### Що перевірити далі (за бажанням)

- Ручний прогон тест-чеклісту (розділ 16).
- Додати результат аналізів у профіль гравця після виконання запиту в Med-Bay (ЗАДАЧА №1).
- Опційно: абсолютні шляхи для JSON у config.

---

## 20. Швидкість завантаження та оцінка сайту

Перевірка завантаження кожного елемента та оцінка сайту в цілому (на основі аналізу коду та стратегій завантаження).

### 20.1 Що вже добре

| Елемент | Оцінка | Пояснення |
|--------|--------|-----------|
| **index.html** | Добре | Мінімум ресурсів: один CSS, три скрипти (session_store, auth, system з `defer`). `get_users.php` не викликається до натискання логіну; preload для fetch додається лише при відкритті модалки. |
| **chapter2/hub.html** | Добре | `preconnect` для Google Fonts; шрифти з `media="print" onload="this.media='all'"` — не блокують рендер. Логін не тягне users до введення коду. |
| **chapter2/profile.html** | Добре | `preconnect` + відкладене завантаження шрифтів; `preload` для get_users.php. QR-бібліотека з CDN з `async`. `timer_sync.js` з `defer`. Квести завантажуються lazy при відкритті модалки «Цілі та місії», не при завантаженні сторінки. |
| **Service Worker** | Добре | Навігація: network-first, fallback у кеш/offline. Дані (get_users, gamestate, quests, helix_data): network-first з no-store, fallback у кеш при помилці. Решта: cache-first з оновленням кешу у фоні — швидкий повторний вхід. |
| **API-запити** | Добре | Кеш users у профілі 2 хв; quests — lazy при відкритті модалки; gamestate — при відкритті модалки місій; ping/post не блокують інтерфейс. |
| **Аудіо** | Добре | Файли (3.mp3, 4.mp3, ambience тощо) не завантажуються до старту відтворення; завантаження по запиту. |

### 20.2 Що може гальмувати

| Сторінка / ресурс | Ризик | Рекомендація |
|-------------------|-------|--------------|
| **Google Fonts** (register, architect, declaration, lore) | Блокує рендер: `<link href="https://fonts.googleapis.com/...">` без `media="print" onload="..."` | Зробити як у hub/profile: `preconnect` + `link` з `media="print" onload="this.media='all'"` + `<noscript>` fallback. |
| **chapter2/personnel.html** | Зовнішня текстура: `url('https://www.transparenttextures.com/...')` — залежність від стороннього сервера і мережі | Опційно: завантажити паттерн локально в `assets/` і підключити з проєкту. |
| **profile.html — відкриття «Цілі та місії»** | Одночасно 3 запити: quests.json, get_leader_votes.php, get_missions.php | Прийнятно (модалка вже відкрита). Якщо потрібно прискорити: preload quests.json при hover на кнопку або при першому завантаженні профілю в фоні. |
| **chapter2/bunker_test.html** | Один великий блок `<script defer>` — весь JS inline | Залишити прийнятним; при росте коду винести в окремий файл з кешуванням. |
| **Аватарки в профілі** | `../uploads/${code}.jpg` без розмірів — велике зображення може сповільнити | Рекомендація: задати width/height або `max-width` у CSS; опційно — thumbnail для списків. |

### 20.3 Типові сценарії завантаження

- **Вхід на index → логін:** HTML + style.css + 3 JS. Users не завантажуються до натискання CONNECT. **Оцінка:** швидко.
- **Вхід на hub → profile:** HTML + CSS + шрифти (відкладені) + 4 JS. Потім get_users.php + ping.php. Квести/місії/таймер — по модалках або таймері. **Оцінка:** добре; найважча сторінка — профіль, але без зайвих запитів до взаємодії.
- **Реєстрація (крок 1 → 2):** get_users при виборі фракції; при переході на крок 2 — audio_config для музики «4». **Оцінка:** прийнятно.
- **K.I.R.A. (test.html?from=register):** style + 2 JS + audio_config при завантаженні. **Оцінка:** швидко.
- **Бункер, біометрична верифікація:** власний HTML/CSS, мінімум зовнішнього; audio_config / API по дії. **Оцінка:** добре.

### 20.4 Оцінка сайту в цілому

- **Перше відкриття (cold):** залежить від мережі та Google Fonts; HTML/CSS/JS невеликі. Типово **1–3 с** до інтерактивного екрану на середньому з’єднанні.
- **Повторне відкриття (з кешем SW):** cache-first для статики дає **швидке** повторне завантаження; дані (users, gamestate, quests) йдуть з мережі.
- **Критичний шлях:** мінімальний — немає обов’язкових великих скриптів у head без defer/async; users не на старті головної та профілю.
- **Рекомендації для подальшого прискорення:** (1) Уніфікувати відкладене завантаження шрифтів на всіх сторінках; (2) опційно — локальна текстура для personnel; (3) розміри/обмеження для аватарок у профілі.

**Висновок:** Сайт орієнтований на швидкий старт: мало блокуючих ресурсів, lazy-завантаження квестів і даних за діями, розумний кеш у Service Worker. Найбільший виграш — узгодити завантаження шрифтів (відкласти на всіх сторінках) і при потребі локалізувати зовнішні ресурси.

---

**Оновлено:** 2026-01-30  
**Версія:** 2.8 — Додано розділ 20: швидкість завантаження кожного елемента та оцінка сайту в цілому (що добре, що може гальмувати, типові сценарії, рекомендації).
